#include <stdio.h>

int main() {
  //Escreva um algoritmo para imprimir os números de 1 (inclusive) a 10 (inclusive) em ordem crescente utilizando a estrutura REPITA e um CONTADOR. 

  int contador = 0;
  do {
    contador++;
    printf("%d\n", contador);
  } while (contador <= 9);

  //Resultado
  //Programa funcionou normalmente imprimindo os números de 1 a 10 em ordem crescente utilizando a estrutura repita e um contador. 
}